[package]
name = "work-queue-worker"
version = "1.0.0"
edition = "2021"
description = "Competing-consumers worker for the Work Queue blog post"

[dependencies]
redis = { version = "1.5", features = ["streams"] }
